<div class="container"> 
	<div class="row">
		<%= form_for(@disease) do |d| %>
		 <% if @disease.errors.any? %>
			<div id="error_explanation">
				<h2><%= pluralize(@disease.errors.count, "error") %> prohibited this disease from being saved:</h2>
				<ul>
					<% @disease.errors.full_messages.each do |message| %>
						<li><%= message %></li>
					<% end %>
				</ul>
			</div>
		<% end %>

		<div class="form-group">
			<%= d.label :nome %><br>
			<%= d.text_field :name , :autofocus => true %>
		</div>
		<div class="form-group">
			<%= d.label :estado %><br>
			<%= d.text_field :state %>
		</div>
		<div class="form-group">
			<%= d.label :quantidade %><br>
			<%= d.number_field :quantity %>
		</div>
		<div class="form-group">
			<%= d.button :submit , name: "gene_id" , value:params[:gene_id]%>
		</div>
		<br><br>
		<% end %>
	</div>
</div>